.venv/
__pycache__/
*.pyc
*.db
*.db-*
*.db.bak-*
.env
.ai-pricing.json
*.egg-info/
.pytest_cache/
*.log
scripts/deploy/deploy.local.env
.claude/settings.local.json

# Epic 10: ceph-upgrade-test-runner-frontend (React/Vite, relocated from
# the throwaway prototype) -- installed deps and build output, not source.
ceph-upgrade-test-runner-frontend/node_modules/
ceph-upgrade-test-runner-frontend/dist/

# Ceph Health dashboard build dependencies/cache. The deterministic
# production bundle under dashboard/static/ceph-health is committed so a
# checkout can serve the page immediately; node_modules is always rebuilt.
ceph-health-dashboard/node_modules/
ceph-health-dashboard/*.tsbuildinfo

# Epic 10 Story 10.2: uploaded baseline files (checksums/dumps) -- operator
# data, not source; saved under the project root by dashboard/routes/
# test_runner.py's baseline-upload route.
test_runner_baselines/
.codex-account/

# Claude Code per-project account/session state (machine id, migration
# flags) — tooling state, not source. Sibling of .codex-account/ above.
.claude-account/
